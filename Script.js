// Function to navigate between pages
function navigateTo(page) {
    const repoName = "birthday-surprise"; // Replace with your GitHub repository name
    window.location.href = `/${repoName}/${page}`;
}
